import * as fs from 'fs'

export const saveConfig = async (config) => {
    console.log(config)
    // try {
    //     await fs.writeFile('./mdr.txt', Buffer.from('mdr'))
    //     await fs.writeFile('./mdr.txt', Buffer.from('mdr'))
    //     await fs.writeFile('./mdr.txt', Buffer.from('mdr'))
    //     await fs.writeFile('./mdr.txt', Buffer.from('mdr'))
    //     await fs.writeFile('./mdr.txt', Buffer.from('mdr'))
    // } catch (error) {
        
    // }
}